---
import Icon from './Icon.astro';
import Button from './Button.astro';

interface BentoCard {
  icon: string;
  title: string;
  description: string;
  span?: 'full' | 'default';
  metric?: { value: string; label: string };
}

const cards: BentoCard[] = [
  {
    icon: 'speed',
    title: 'Velocidade Extrema (Zero-JS)',
    description: 'Entregamos HTML/CSS puros que carregam instantaneamente.',
    span: 'default',
  },
  {
    icon: 'search',
    title: 'Escala Infinita',
    description: 'Criamos centenas de páginas locais otimizadas para IA (GEO) para dominar buscas de cauda longa.',
    span: 'default',
  },
  {
    icon: 'auto_graph',
    title: 'Manutenção Evolutiva',
    description: 'Sem taxa de setup. Você assina uma parceria de performance com otimização contínua de CRO.',
    span: 'default',
  },
];
---

<section class="grid grid-cols-1 md:grid-cols-3 gap-6">
  {cards.map((card) => (
    <div class:list={[
      'bento-border bg-zinc-950/80 backdrop-blur-[12px] p-8 rounded-lg relative overflow-hidden group',
      card.span === 'full' ? 'md:col-span-2' : '',
      card.span === 'full' && card.metric ? 'md:col-span-3' : '',
    ]}>
      <div class="absolute inset-0 bg-gradient-to-br from-primary/10 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-150"></div>
      
      <Icon name={card.icon} class="text-primary mb-4 text-3xl" size="36px" />
      
      <h3 class="font-headline-h3 text-headline-h3 mb-2">{card.title}</h3>
      
      <p class="font-body-sm text-body-sm text-text-secondary max-w-md">
        {card.description}
      </p>

      {card.metric && (
        <div class="mt-8 font-technical-mono text-technical-metric text-primary">
          {card.metric.value}
        </div>
      )}
    </div>
  ))}
</section>